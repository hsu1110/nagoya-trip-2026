# 名古屋/京都行程網頁：資料擴充指南

這個網頁的靈魂在於程式碼下方的 `const db = [ ... ];` 區塊。這裡就是你的「微型資料庫」。未來如果有新的想去的店，或是想調整筆記，只要修改這裡面的內容即可。

## 📝 新增一筆資料需要準備的 9 項資訊

當你發現了一間新的必吃美食或景點，請準備好以下資訊，並複製這個「模板」貼到 `db` 陣列的最後面（記得在前一個大括號 `}` 後面加逗號 `,`）：

### 🍔 美食專用模板

```javascript
{ 
  id: 99, // 【序號】給它一個不重複的數字
  area: "名站區", // 【區域】必須是上方按鈕有出現的區域名稱 (如: 大須 / 榮商圈、熱田神宮區)
  type: "food", // 【類型】美食固定填 "food"，這會決定它出現在哪一區塊
  name: "餐廳名稱", // 【標題】顯示在卡片上的大標題
  hours: "11:00–22:00", // 【時間】營業時間
  location: "大約位置", // 【地點】例如："名站東口步行 5 分鐘"
  category: "晚餐 / 居酒屋", // 【分類】這間店適合什麼時候吃？或是什麼料理？
  notes: "必點手羽先跟啤酒！", // 【筆記】你的私人提醒、必吃菜色或排隊攻略
  icon: "fa-utensils", // 【圖示】(參考下方圖示挑選指南)
  color: "bg-orange-100 text-orange-600", // 【配色】(參考下方配色挑選指南)
  mapQuery: "日本當地的精準日文店名" // 【地圖搜尋】Google Maps 導航用的精準關鍵字
}
```

### 🏯 景點專用模板

景點的模板跟美食幾乎一樣，只有兩個地方不同（`type` 變成 attraction，`category` 變成 transport）：

```javascript
{ 
  id: 100, 
  area: "京都 / 市郊", 
  type: "attraction", // 【類型】景點固定填 "attraction"
  name: "新景點名稱", 
  hours: "09:00-17:00", 
  location: "大概位置", 
  transport: "從某某站搭乘某某線 5 分鐘", // 【交通】抵達方式
  notes: "這裡很好拍照...", 
  icon: "fa-camera", 
  color: "bg-blue-100 text-blue-600", 
  mapQuery: "精準日文景點名" 
}
```

---

## 🎨 裝飾指南：如何挑選圖示 (icon) 與配色 (color)？

為了讓你的網頁看起來五彩繽紛且直覺，新增資料時可以自由搭配顏色與圖示。

### 1. 常用圖示代碼 (icon)
網頁使用的是 FontAwesome 6.4.0 圖示庫，你只要替換 `fa-` 後面的英文即可：
* **美食類：** `fa-utensils` (餐具)、`fa-bowl-rice` (米飯/丼飯)、`fa-bowl-food` (拉麵/湯麵)、`fa-fish` (海鮮/壽司)、`fa-fire-burner` (燒肉)、`fa-mug-hot` (咖啡/熱飲)、`fa-cake-candles` (甜點)、`fa-pepper-hot` (辛辣/味仙)、`fa-bread-slice` (吐司)。
* **景點類：** `fa-camera` (照相機)、`fa-store` (商店/逛街)、`fa-bag-shopping` (購物袋/百貨)、`fa-torii-gate` (神社/鳥居)、`fa-chess-rook` (城堡)、`fa-tree` (公園/大自然)、`fa-train` (車站/鐵道)、`fa-landmark` (地標)。

### 2. 常用配色代碼 (color)
直接複製整串貼到 `color` 欄位中，可以讓每張卡片的圖示有不同的背景色：
* 🔴 **紅色系 (燒肉/熱情)：** `bg-red-100 text-red-600`
* 🟠 **橘色系 (炸物/居酒屋)：** `bg-orange-100 text-orange-600`
* 🟡 **黃色系 (麵包/咖啡)：** `bg-yellow-100 text-yellow-600`
* 🟢 **綠色系 (神宮/抹茶/自然)：** `bg-green-100 text-green-700`
* 🔵 **藍色系 (海鮮/百貨/車站)：** `bg-blue-100 text-blue-600`
* 🟣 **紫色系 (次文化/動漫/特殊)：** `bg-purple-100 text-purple-600`
* 🩷 **粉色系 (甜點/網美點)：** `bg-pink-100 text-pink-500`

---

## 🗺️ Google Maps 精準導航技巧 (mapQuery)
在 `mapQuery` 欄位中，建議填寫**「日文全名」**或**「英文官方名稱」**，這樣導航最精準：
* ✅ 推薦：`飛騨牛一頭家 馬喰一代 名古屋WEST`
* ❌ 避免：`馬喰一代燒肉` (可能會搜到其他縣市的分店)